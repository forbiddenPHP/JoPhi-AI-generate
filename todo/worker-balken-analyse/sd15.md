# sd15 Worker — Progress-Bar Analyse

**Entry Point:** `worker/sd15/generate.py`

## tqdm-Setup

Kein tqdm-Import vorhanden. Kein globaler Override.
Die diffusers StableDiffusionPipeline nutzt intern tqdm fuer Denoising-Steps, aber ohne `file=sys.stderr` Override landen die Balken in Pipes nicht auf stderr.

---

## Step-Analyse

| Step | Label vorhanden? | Balken vorhanden? | Was fehlt |
|------|:-:|:-:|-----------|
| ControlNet laden (optional) | Ja (Z.157) | Nein | Simulierter Balken fehlt |
| Checkpoint laden | Ja (Z.166) | Nein | Simulierter Balken fehlt |
| Attention Slicing | Nein | Nein | Trivial, kein Balken noetig |
| LoRA(s) laden | Ja (Z.189, pro LoRA) | Nein | Simulierter Balken fehlt |
| Denoising / Inference | Ja (Z.207) | Nein (intern hat pipe() tqdm, aber ohne stderr-Force) | tqdm-Override fehlt |
| Bild speichern | Ja (Z.230) | Nein | Trivial, kein Balken noetig |

## Handlungsbedarf

1. Globalen tqdm-Override einrichten (wie ltx2) damit die diffusers-Denoising-Balken auf stderr sichtbar werden
2. Simulierte Balken fuer Model-Laden, ControlNet-Laden, LoRA-Laden

### Vorschlag: tqdm-Override (nach Zeile 14, vor torch import)

```python
import tqdm as _tqdm_mod, tqdm.auto as _tqdm_auto
_OrigTqdm = _tqdm_mod.tqdm
class _ForceTqdm(_OrigTqdm):
    def __init__(self, *a, **kw):
        kw["file"] = sys.stderr
        kw["disable"] = False
        super().__init__(*a, **kw)
        self.disable = False
        self.refresh()
_tqdm_mod.tqdm = _ForceTqdm
_tqdm_auto.tqdm = _ForceTqdm
```

Mit diesem Override werden die 20 Denoising-Steps als echter tqdm-Balken auf stderr sichtbar. Das ist der wichtigste Gewinn fuer diesen Worker.

### Vorschlag: Checkpoint laden (Zeile 166-179)

```python
from tqdm import tqdm
# Zeile 166: print("Loading checkpoint …", ...) — BLEIBT
_bar = tqdm(total=1, desc="Loading checkpoint", file=sys.stderr); _bar.refresh()
if controlnet is not None:
    pipe = StableDiffusionControlNetPipeline.from_single_file(...)
else:
    pipe = StableDiffusionPipeline.from_single_file(...)
_bar.update(1); _bar.close()
```

### Vorschlag: ControlNet laden (Zeile 157-163)

```python
# Zeile 157: print(f"Loading ControlNet ({args.controlnet_mode}) …", ...) — BLEIBT
_bar = tqdm(total=1, desc=f"Loading ControlNet ({args.controlnet_mode})", file=sys.stderr); _bar.refresh()
controlnet = ControlNetModel.from_pretrained(...)
_bar.update(1); _bar.close()
```

### Vorschlag: LoRA laden (Zeile 185-193)

```python
if lora_specs:
    _bar = tqdm(total=len(lora_specs), desc="Loading LoRAs", file=sys.stderr); _bar.refresh()
    for name, intensity in lora_specs:
        lora_path = _resolve_lora_path(name)
        if lora_path:
            print(f"  Loading LoRA: {name} (intensity: {intensity}) …", file=sys.stderr)
            pipe.load_lora_weights(str(lora_path), adapter_name=...)
            loaded_loras.append(...)
        _bar.update(1)
    _bar.close()
```

### Vorschlag: Inference (Zeile 207-222)

Mit dem tqdm-Override oben braucht es hier keinen simulierten Balken — die internen diffusers-Steps werden automatisch angezeigt. Das bestehende Label (Z.207) reicht als Ankuendigung.
